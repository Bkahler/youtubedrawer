<div>
  <div class="portfolioFilter">
    <%if current_user%>
    <a href="#" data-filter=".results" class="current">Search Results</a>
    <%current_user.playlists.each do |playlist|%>
    <a href="#" data-filter=".<%= (playlist.title.gsub(/\s+/, "")).downcase %>"> <%= playlist.title %></a>
    <%end%>
    <%end%>
  </div>
</div>